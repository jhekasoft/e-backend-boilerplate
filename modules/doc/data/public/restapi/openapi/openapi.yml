openapi: 3.0.3
info:
  title: e-backend-boilerplate
  description: e-backend-boilerplate REST API 😼 | [Documentation](/doc)
  version: 0.0.7
tags:
  - name: Health
    description: Health API
  - name: Auth
    description: Authorization API
  - name: CV
    description: Data for Curriculum vitæ (résumé)
paths:
  /health:
    $ref: "health/health.yml"

  /auth/users:
    $ref: "auth/users.yml"
  /auth/signin:
    $ref: "auth/signin.yml"
  /auth/users/me:
    $ref: "auth/users-me.yml"

  /admins:
    $ref: "admins/admins.yml"
  /admins/{id}:
    $ref: "admins/admins-id.yml"

  /cv/developer-timeline:
    $ref: "cv/developer-timeline.yml"
  /cv/cv:
    $ref: "cv/cv.yml"
  /cv/latex:
    $ref: "cv/latex.yml"
  
  /tamagluchi:
    $ref: "tamagluchi/tamagluchi.yml"
  /tamagluchi/calculate:
    $ref: "tamagluchi/calculate.yml"

  /sum/articles:
    $ref: "sum/articles.yml"
  /sum/articles/{word}:
    $ref: "sum/articles-word.yml"
  /sum/import:
    $ref: "sum/import.yml"
components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
  schemas:
    UnauthorizedResponse:
      type: object
      properties:
        Message:
          type: string
          example: "missing or malformed jwt"
    NotFoundResponse:
      type: object
      properties:
        Message:
          type: string
          example: "not found"
    Model:
      type: object
      properties:
        ID:
          type: integer
          format: uint32
          example: 1
        CreatedAt:
          type: string
          format: date-time
          example: "2023-01-01T00:00:00Z"
        UpdatedAt:
          type: string
          format: date-time
          example: "2023-01-01T00:00:00Z"
        DeletedAt:
          type: string
          format: date-time
          nullable: true
          example: "2023-01-01T00:00:00Z"
